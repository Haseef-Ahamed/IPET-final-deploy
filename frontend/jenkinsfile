pipeline {
    tools {
        nodejs "Node 18" 
    }

    agent any

    stages {
        stage('Build') { 
            steps {
                dir('frontend') {
                     sh 'npm install'
                    //  sh 'npm run build'
                }
            }
        }
    }
}